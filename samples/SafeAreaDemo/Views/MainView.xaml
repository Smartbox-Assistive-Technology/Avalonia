<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SafeAreaDemo.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="800"
             d:DesignHeight="450"
             x:Class="SafeAreaDemo.Views.MainView"
             x:DataType="vm:MainViewModel"
             TopLevel.AutoSafeAreaPadding="{Binding AutoSafeAreaPadding, Mode=TwoWay}">
  <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
    <TextBlock FontSize="24" FontFamily="Arial Rounded MT">Hello, with Arial Rounded MT</TextBlock>
    <TextBlock FontSize="24" FontFamily="Arial Rounded MT Bold" Margin="0,20,0,0">Hello, with Arial Rounded MT Bold</TextBlock>
  </StackPanel>
</UserControl>
